# Polis — 실행 로드맵 (v2, 완성 우선)

> **v1과의 차이:** 목표를 학습 우선 → **완성 우선**으로 전환. 이에 따라
> ① 마일스톤 순서 재배치(시각화를 최우선으로), ② M1-b 재정의(코드 실상에 맞춤),
> ③ 영속 스레드를 별도 마일스톤으로 격리, ④ M4~M6 잘라냄.
>
> 판단 근거는 9절 "잘라낸 것과 그 이유"에 기록.

---

## 0. 진행 원칙

- **완성이 1순위.** 눈에 보이는 결과물이 없으면 포트폴리오가 아니라 로컬 폴더다.
- 범위가 흔들리면 "이게 화면에 보이는 결과물에 기여하는가"로 판단하고, 아니면 자른다.
- 결정론적 레이어끼리는 **수치 동일성**, 비결정 레이어는 **트렌드 일관성**으로 검증.
- 작게 쪼개서 하나씩 확인. 한 번에 여러 겹을 얹지 않는다.
- 용어는 `CLAUDE.md` 3절 정의만 사용한다(특히 "영속 스레드").

---

## 1. 전체 지형도

```
[엔진 검증 완료] ──────── [보이는 결과물] ──────── [서사 완성]

M0  순차 동기 엔진            ✅ 결정론적 앵커
M1-a 틱 병렬                  ✅ M0와 수치 동일
M1-b 틱 병렬 + 메일박스        ✅ (재정의 완료)
 │
 ▼
M2  실시간 시각화              ⬅ 지금 여기 · 최우선
 │   Spring Boot + WebSocket → React/Canvas
 ▼
M3  규칙 전략 패턴              질문을 갈아끼우는 관문
 ▼
M4  영속 스레드 구현체 + 3모델 비교
 │   포트폴리오 서사를 완성시키는 구간
 ▼
M5  자유 확장 실험장            극단화 · 집단 변수 등
 ▼
M6  (선택) Python 분석 레이어

[잘라냄]  외부 충격(3단계)
```

> **M3을 선택 → 필수로 올린 이유.** 취업 축("확장 가능한 아키텍처")과 흥미 축
> ("질문을 갈아끼운다")이 완전히 겹치는 유일한 지점이다. 나중에 붙이면 엔진을
> 뜯게 되지만 지금 하면 인터페이스 정리 수준이라, 시점이 이를수록 싸다.
> M4와 M5는 M3 이후 순서를 바꿔도 무방하다.

---

## 2. ✅ M0 — 순차 동기 엔진 (완료)

이후 모든 비교의 기준점. **손대지 않는다.**

- [x] 동화+반발 규칙 단일 스레드 구현
- [x] 더블 버퍼링
- [x] 파라미터 스윕 — 임계값 ~1.2 부근 상전이 확인
- [x] 콘솔에서 양극화(두 봉우리) 확인

**미완:** M0 고정 시드 결과(분산 추이, 극단값 비율 시계열)를 `baseline/m0.json`으로
저장. M3 트렌드 비교의 앵커가 되므로 M3 착수 전까지는 반드시 있어야 한다.

---

## 3. ✅ M1-a — 틱 병렬 (완료)

- [x] `invokeAll`로 틱 단위 병렬화
- [x] M0와 수치 동일성 검증
- [x] 공유 카운터 레이스 진단 → `LongAdder`

**한 줄 확인:** 왜 `AtomicLong`이 아니라 `LongAdder`인가? → 고경합 카운터에서 셀
분산으로 경합을 줄이기 때문.

---

## 4. ✅ M1-b — 틱 병렬 + 메일박스 (재정의 후 완료)

> **재정의 사유.** v1의 M1-b는 "행위자 1명 = 영속 스레드 1개"를 명세했으나, 실제
> `Simulation.java`는 매 틱 `invokeAll`로 태스크를 제출하는 구조다. 즉 M1-a 골격에
> 메일박스를 얹은 것이며, 배리어가 그대로 있으므로 영속 스레드 모델이 아니다.
> 영속 스레드는 M3으로 분리하고, M1-b는 **메시지 전달 구조 도입까지**로 범위를 줄여
> 완료 처리한다.

- [x] `Agent`가 `BlockingQueue` 메일박스 보유
- [x] 메시지로 의견 전달 → `UpdateRule` 적용
- [x] `Agent` 리스트로 N명 일반화
- [x] `NeighborSelector` 분리 (시드 고정으로 이웃 시퀀스 재현 확인)
- [x] 직접 참조 라우팅
- [x] executor 기반 종료 (`InterruptedException` → `break` → `shutdownNow`)

### M1-b 마무리 작업 (M2 착수 전 30분)

- [x] **`testcode.java` 정리** — 삭제함(2026-09-09). 어디서도 참조되지 않는
      `LinkedBlockingQueue<String>` put/take 스크래치였고 애초에 `.gitignore`에 걸려
      추적도 안 되던 파일. 실제 영속 스레드 프로토타입은
      `experimental/PersistentThreadSimulation.java`로 정식 대체됨.
      _왜:_ 정체 불명 스크래치 파일이 남아 있으면 코드 리딩 시 오진단이 반복된다.
- [x] **N 스케일업 확인** (2026-09-09) — 500 / 5,000 / 50,000으로 확인. 셋 다 데드락·에러 없이
      종료됐고, threshold=1.5에서 분산이 0.33→0.16→0.06으로 줄고 극단값 비율이 0%로 수렴하는
      동일한 거시 패턴을 모든 규모에서 재현함. N=50,000/tick 100에서 17.5초 소요 — N 증가 대비
      비선형으로 느려지는 경향 관찰(틱마다 2×N개 가상 스레드 태스크 + 람다/리스트 재할당 비용 추정,
      추가 프로파일링은 필요시 M4에서).
- [x] **M0 baseline JSON 저장** (2026-09-09) — `BaselineRunner`(`com.sys.polis.BaselineRunner`)로
      `baseline/m0.json` 생성. AGENT_COUNT=100, TICK_COUNT=100, mu=0.01, threshold=1.5,
      seed(100/42) 고정, 매 틱 분산·극단값 비율 기록.
      _과정에서 발견한 버그:_ 같은 시드로 반복 실행해도 분산 값이 미세하게 달랐음(비결정성).
      원인은 한 틱에 여러 발신자가 같은 수신자를 고를 수 있는데(같은 시드에서도 발생), mailbox에
      도착하는 순서가 가상 스레드 스케줄러에 좌우되고 `AttractionRepulsionRule.update()`가 순서에
      따라 다른 값을 내는 데 있었다. 게다가 mailbox가 `BlockingQueue<Double>`이라 발신자 id 자체를
      저장하지 않아 정렬할 키도 없었다(수신 측이 항상 `self.getId()`로 잘못 라벨링).
      `agent/Message.java`(CLAUDE.md에 명세돼 있었지만 누락돼 있던 클래스)를 추가해 발신자 id를
      함께 담고, `Simulation` Phase 2에서 받은 메시지를 발신자 id로 정렬한 뒤 규칙을 적용하도록
      고쳐 완전한 재현성을 확보(세 번 반복 실행 결과가 바이트 단위로 동일함을 확인).
      이 수정으로 M1-a "M0와 수치 동일성 검증" 체크가 딛고 있던 전제(발신 순서 비의존)가
      비로소 실제로 성립하게 됨 — mailbox·NeighborSelector 도입 리팩터링 이후 재검증이 빠져있던 구멍.

---

## 5. ⬅ M2 — 실시간 시각화 (최우선)

> **여기가 프로젝트의 존재 여부를 가른다.** 지금까지의 M0~M1-b는 전부 콘솔 숫자다.
> 화면에서 분포가 갈라지는 것을 보여줄 수 있어야 비로소 포트폴리오가 된다.
>
> ⚠️ **범위 경보:** 주특기(Spring/React) 구간이라 과잉 엔지니어링에 빠지기 쉽다.
> 인증·DB·배포·Docker 전부 넣지 않는다.

- [x] **M2-1 엔진을 라이브러리 모듈로 분리** (2026-09-10)
  - Gradle 루트를 `polis-engine`(순수 Java, `java-library`+`application`)과
    `polis-server`(Spring Boot, `polis-engine`에 `implementation(project(":polis-engine"))`로만
    의존)로 쪼갬. 기존 `com.sys.polis.*`(Main, BaselineRunner, `polis_engine.*`)는 전부
    `git mv`로 `polis-engine/`에 이동 — 파일 내용은 손대지 않아 M0/M1 앵커가 그대로 보존됨.
  - _검증(엔진 Spring 의존성 0개):_ `./gradlew :polis-engine:dependencies --configuration
    runtimeClasspath` → `No dependencies`. `grep -i spring` 매치 0건.
  - _검증(M0 앵커 무손상):_ 모듈 분리 전 `baseline/m0.json`을 백업해두고, 분리 후
    `:polis-engine:runBaseline`으로 재생성해 `diff` — **바이트 단위로 동일**. `:polis-engine:run`
    (Main.java)도 재구조화 후 그대로 실행해 콘솔 양극화(분산 0.31→0.15→0.057, 극단값
    20%→2%→0%) 재현 확인.
  - _곁가지:_ CLAUDE.md 패키지 구조에 명세돼 있었지만 비어 있던 `metric/` 패키지를
    `MetricCollector`(히스토그램/분산/극단값 비율 + `Snapshot` 레코드)로 채움. M0 앵커
    (Main.java/BaselineRunner.java)는 "손대지 않는다" 원칙 때문에 이 유틸로 갈아끼우지 않고
    독립 구현으로 남겨둠 — `MetricCollectorTest`가 두 계산이 같은 결과를 낸다는 것만 확인.

- [x] **M2-2 WebSocket(STOMP) 연결 확인** (2026-09-10)
  - `polis-server`에 `WebSocketConfig`(엔드포인트 `/ws`, SockJS 없이 순수 WebSocket — 프론트가
    아직 없어 KISS) + `ConnectionAckListener`(누군가 `/topic/handshake`를 구독하면 더미
    `{"status":"connected"}` 1회 push).
  - _검증:_ 프론트가 없어 Spring `WebSocketStompClient`로 자동화된 통합 테스트
    (`HandshakeWebSocketTest`)를 작성 — STOMP로 연결→구독하면 5초 내 더미 메시지 1회 도착.
    **PASS.**
  - _설계 함정 하나 발견:_ 처음엔 `SessionConnectedEvent`(CONNECT 시점)에 바로 쐈는데, 클라이언트가
    아직 구독을 마치기 전이라 Simple Broker가 메시지를 버리고 테스트가 실패했다(구독자 없는 destination에
    쏘면 그냥 사라짐 — 버퍼링 안 해줌). `SessionSubscribeEvent`(구독 시점)로 옮겨서 해결.

- [x] **M2-3 매 틱 스냅샷 push** (2026-09-10)
  - `SimulationSnapshotBroadcaster`: 누군가 `/topic/snapshots`를 구독하는 순간(=M2-4 제어 API가
    아직 없어 "서버 기동 즉시 발사" 대신 택한 최소한의 시작 트리거) `application.yml` 파라미터로
    시뮬레이션을 1회 실행하며 매 틱 `MetricCollector.snapshot(tick, agents)`(히스토그램 20버킷 +
    분산 + 극단값 비율만, raw opinion 없음)을 push.
  - _M2-4에서 대체됨:_ 이 클래스는 M2-4 착수 시 `SimulationRunner`로 갈아끼우고 삭제했다 —
    "구독=발사 트리거"였던 이 절 자체가 제어 API가 생기기 전까지의 임시방편이었기 때문(아래
    M2-4 항목 참조). 발행 로직(스냅샷 계산·publish) 자체는 그대로 옮겨졌다.
  - _검증(순서·무손실·집계전용, N=100/tick=200):_ `SnapshotBroadcastTest` — 201개 스냅샷이 tick
    순서대로 전부 도착(유실 없음), 매 틱 히스토그램 합이 agentCount와 일치(raw 값 아님 확인),
    threshold=1.5에서 분산이 초기→최종으로 줄어드는 M0 수렴 트렌드 재현. **PASS.**
  - _검증(N=10000, "프레임 드랍 없이 흐르는가"):_ `LargeScaleSnapshotBroadcastTest` — agent-count
    10,000/tick-count 50으로 51개 스냅샷 전부 유실 없이 도착. **PASS** (약 5~6초 소요, Spring 컨텍스트
    기동 포함). *주의:* 아직 Canvas 프론트(M2-5)가 없어 "프레임 드랍"의 실제 렌더링 의미는 검증 못 함 —
    여기선 WebSocket 계층에서 스냅샷 유실이 없는지를 근사치로 확인한 것.
  - _진짜로 발견한 버그(경합 재현):_ N=10000 테스트 직후 곧바로 N=100 테스트를 돌리면(같은 JVM,
    두 Spring 컨텍스트가 근접한 시점에 기동/종료) tick 29/30/31이 뒤섞여 도착하며 순서 검증이
    간헐적으로 깨졌다. 원인은 Spring 기본 `clientOutboundChannel`이 스레드 풀(멀티 스레드)로
    클라이언트에 전달하는 구조라, 발행 스레드(가상 스레드 1개)가 순서대로 publish해도 부하가 걸리면
    브로커가 워커 스레드 여러 개로 나눠 보내면서 도착 순서가 어긋날 수 있다는 것. `WebSocketConfig`에
    `configureClientOutboundChannel`을 추가해 코어/맥스 풀 사이즈를 1로 고정해 "발행 순서 = 도착
    순서"를 강제했고, 같은 조합을 3회 반복 실행해 재발하지 않음을 확인. (관측된 게 "사회 현상"이 아니라
    "메시징 인프라의 부산물"이라는 점에서 M4-4 경고와 같은 종류의 함정.)

- [x] **M2-4 제어 API (start / stop / 파라미터 변경)** (2026-09-14)
  - `SimulationRunner`(싱글턴, 실행 상태 보유)를 `SimulationController`가 REST로 조작한다:
    `POST /api/simulation/start`(agentCount/mu/threshold/tickCount 전부 선택값), `POST .../stop`,
    `POST .../params`(mu/threshold만), `GET .../status`. 구독은 더 이상 발행 트리거가 아니다
    (M2-3의 "구독하면 발사"는 제어 API가 없던 동안의 임시방편이었다 — 위 M2-3 항목 참조).
  - _mu/threshold vs N, 왜 다르게 다루나:_ mu/threshold는 돌고 있는 `AttractionRepulsionRule`
    인스턴스의 필드를 세터로 바꾸는 것뿐이라 opinion 상태를 유지한 채 다음 틱부터 즉시 반영되는
    진짜 "실행 중 변경"이다. agentCount(N)는 Agent 리스트 크기 자체를 바꾸는 것이라 살아있는
    인구에 의미 있게 적용할 규칙이 없어(그건 "출생/사망" 모델이라는 별도 기획이 필요) `/start`를
    다시 호출해 처음부터 재시작하는 방식으로만 지원한다. `AttractionRepulsionRule`의 mu/threshold
    필드를 `final` → `volatile`로 바꾸고 검증 로직을 갖춘 `setMu`/`setThreshold`를 추가했다
    (`Simulation`의 receiveTasks가 여러 가상 스레드에서 이 필드를 동시에 읽는 와중에 제어 API
    호출 스레드가 값을 바꿀 수 있어 가시성 보장이 필요).
  - PATCH가 의미상 더 맞지만 JDK 기본 `HttpURLConnection`이 PATCH 메서드를 지원하지 않아
    (`ProtocolException: Invalid HTTP method: PATCH`) start/stop과 통일해 POST로 뒀다.
  - _검증(생명주기):_ `SimulationControlTest` — start 직후 tick 0 스냅샷 도착·`status().running()`
    true, stop 후 `running()` false. **PASS.**
  - _검증(mu/threshold 라이브 반영):_ 실행 중에 `/params`로 threshold를 바꾼 뒤 `/status`로 즉시
    반영을 확인(재시작 없이, `agentCount`도 그대로임을 함께 확인). **PASS.** ("임계값을 바꾸면
    거동이 바뀐다"는 plan의 검증 문구는 M0/M1에서 이미 수치로 확인된 사실이라 재입증하지 않고,
    여기서는 "제어 API가 실행 중인 규칙에 실제로 닿는가"를 확인했다 — React+Canvas가 붙는
    M2-5에서야 "화면 거동"으로 직접 관찰 가능해진다.)
  - _검증(N 변경=재시작):_ agentCount=30으로 시작 후 `/start`에 agentCount=80만 보내 재시작하면
    `status().agentCount()`가 80으로 바뀌고 새 스냅샷의 히스토그램 합도 80으로 바뀜을 확인. **PASS.**
  - _진짜로 발견한 버그 1(널 바디 415):_ `RestTemplate.postForObject(url, null, ...)`은 빈 폼
    (`application/x-www-form-urlencoded`)으로 POST해버려서, JSON `@RequestBody`를 기대하는
    `/start`가 415로 거부했다. `TestRestTemplate`은 기본적으로 4xx/5xx에도 예외를 던지지 않아서
    (`runner.start()`가 아예 호출되지 않았는데도) 증상은 "tick 0가 5초 내 안 옴"으로만 보여 원인을
    한 번 더 파고들어야 했다 — 빈 JSON 객체(`Map.of()`)로 보내도록 테스트를 고쳤다.
  - _진짜로 발견한 버그 2(싱글턴 상태 오염):_ `SimulationRunner`가 싱글턴 빈이라 테스트 메서드
    사이에 실행 상태가 남는다. 한 테스트가 도중에 실패해 정리(`/stop`) 코드를 못 타면, 다음
    테스트가 구독하자마자 "자기가 막 시작한" 스냅샷이 아니라 이전 테스트가 계속 흘리던 스냅샷을
    집어 agentCount 단언이 엉뚱하게 깨졌다. `SimulationControlTest`에
    `@DirtiesContext(AFTER_EACH_TEST_METHOD)`를 붙여 메서드마다 컨텍스트를 새로 띄우는 방식으로
    완전히 격리했다.
  - _진짜로 발견한 버그 3(정지 후 배압 잔량):_ stop()이 계산 루프를 확실히 멈춰도, outbound
    채널(단일 스레드, M2-3에서 고정)이 그 전까지 이미 큐에 쌓인 스냅샷을 뒤늦게 마저 배출한다 —
    계산이 WebSocket 전송보다 훨씬 빨라서 stop 호출 시점엔 이미 여러 틱 분량이 큐에 있을 수 있기
    때문. "멈췄다"의 판정을 "즉시 조용해짐"이 아니라 "백로그가 다 빠진 뒤에도 계속 조용함"으로
    고쳐서(2초 무응답까지 배수 후 다시 2초 확인) 검증했다. N 변경 재시작 테스트도 같은 이유로
    "재시작 후 첫 스냅샷"이 아니라 "새 인원수로 된 첫 스냅샷"을 찾도록 고쳤다. (M2-3의 "정보
    과부하가 아니라 메시징 인프라의 부산물" 경고와 같은 종류의 함정.)
  - _시도했다가 접은 것(STOMP RECEIPT):_ 구독-후-REST호출 경합(구독 프레임이 브로커에 등록되기
    전에 REST 호출이 먼저 도착해 첫 스냅샷을 놓침)을 정확히 없애려고 STOMP RECEIPT 헤더 +
    `TaskScheduler`를 붙여봤으나, 이 클라이언트/브로커 조합에서는 RECEIPT 프레임이 돌아오지
    않아(5초 타임아웃으로 관측) 포기했다. `StompTestSupport.subscribeAndSettle()`로 구독 후
    300ms 유예를 두는 실용적 방식으로 대체 — 로컬 인메모리 브로커의 구독 등록은 밀리초 단위라
    이 정도로 충분함을 반복 실행(연속 3회)으로 확인.

- [ ] **M2-5 React + Canvas 히스토그램**
  - 일반 DOM 아님 — Canvas. 시간에 따라 봉우리가 갈라지는 것 관찰.
  - _검증:_ 콘솔에서 봤던 양극화가 화면에서 같은 모양으로 전개되는가.

- [ ] **M2-6 데모 녹화**
  - 30초~1분 GIF 또는 mp4. README 최상단에 삽입.
  - _왜:_ 채용 담당자는 저장소를 클론해서 실행해 보지 않는다. 움직이는 그림 하나가
    코드 3000줄보다 먼저 읽힌다. **이 항목을 건너뛰지 말 것.**

**완료 게이트:** 파라미터를 바꾸면 화면의 분포가 실시간으로 갈라지거나 뭉친다.
README에 그 장면이 GIF로 박혀 있다.

---

## 6. M3 — 규칙 전략 패턴

> 질문을 갈아끼우는 관문. 여기까지 되면 이후 확장은 전부 "구현체 하나 추가"가 된다.

- [ ] **M3-1 UpdateRule 전략 인터페이스 확정** — 엔진이 인터페이스 하나만 알도록
- [ ] **M3-2 두 번째 구현체로 검증** — 아무 규칙이든 하나 더 만들어 교체가 실제로 되는지 확인
- [ ] **M3-3 UI에서 규칙 전환** — 화면에서 버튼으로 모델 교체

**완료 게이트:** 코드 변경 없이 구현체 교체만으로 다른 거시 패턴이 나온다.

---

## 7. M4 — 영속 스레드 구현체 + 3모델 비교

> 포트폴리오 서사를 완성시키는 구간. **메인 라인을 대체하는 것이 아니라, 네 번째
> `Simulation` 구현체를 추가하는 것이다.** 시각화·규칙 교체는 계속 틱 병렬 위에서 돈다.

- [ ] **M4-1 Simulation 인터페이스 추출**
  - 기존 틱 병렬 구현을 `TickParallelSimulation`으로, 인터페이스는 `Simulation`.
  - _왜:_ 지금이 두 번째 구현체가 실제로 생기는 시점이므로, 여기서 인터페이스를 뽑는 것이 근거 있는 리팩터링이다.
  - _검증:_ 인터페이스 추출 전후 M0 수치 동일성 유지.

- [ ] **M4-2 PersistentThreadSimulation 구현**
  - 행위자마다 무한 루프 가상 스레드. `mailbox.take()`에서 park. **전역 배리어 없음.**
  - _왜 가상 스레드가 여기서만 필수인가:_ 수만 개가 동시에 블로킹 대기해야 한다.
    OS 스레드로는 스택 메모리 때문에 불가능하고, park된 가상 스레드는 힙 객체일 뿐이다.
    이 문장이 프로젝트 전체에서 가상 스레드를 정당화하는 유일한 근거다.
  - _검증:_ 데드락 없이 시간 기반 종료. 여러 시드에서 M0와 같은 방향의 거시 패턴.

- [ ] **M4-3 유계 큐 전환 & 포화 재현**
  - 무제한 큐 → bounded queue. 생산-소비 속도 차를 만들어 큐가 차는 순간을 로그로 포착.
  - _왜:_ 무제한 큐는 문제를 메모리로 미룰 뿐이다. 포화를 눈으로 봐야 정책이 의미를 가진다.

- [ ] **M4-4 백프레셔 정책 비교 (블로킹 / 드롭 / 병합)**
  - 세 정책이 거시 결과(수렴 속도, 양극화 모양)를 어떻게 바꾸는지 기록.
  - _주의:_ 여기서 관측되는 큐 길이는 사회 현상이 아니라 **JVM 스케줄러의 부산물**이다.
    "정보 과부하를 모델링했다"고 말하지 않는다. "구현 레벨의 흐름 제어를 실험했다"가 정확하다.

- [ ] **M4-5 3모델 비교 표 작성**
  - 축: 처리량, 메모리, 라이브 객체 수, 결정성, 백프레셔 발생 여부, 스냅샷 가능 여부.
  - _예상 결과:_ 순수 성능은 틱 병렬이 우세할 가능성이 높다. **그 결과가 나와도 그대로
    쓴다.** "가상 스레드가 이 워크로드에서는 이점이 없고 그 이유는 이렇다"고 말할 수
    있는 것이 이 프로젝트의 최종 결론이며, 가장 방어 가능한 지점이다.
  - _검증:_ README에 표와 함께 3~5문장 결론 서술.

**완료 게이트:** "왜 이 워크로드에서 가상 스레드가 유리/불리한가"에 수치를 들어
답할 수 있다.

---

## 8. M5 — 자유 확장 실험장

> M3이 되어 있으면 항목 하나당 하루 이틀짜리다. 체크리스트로 못 박지 않고 열어둔다.
> 흥미로 하는 일에 진도표를 씌우면 재미가 줄어든다.

후보:

- 극단화 — `EchoChamberRule`. 비슷한 의견의 이웃을 더 자주 만나도록.
- 집단 변수 — `AgentState`에 속성 필드 추가 + 비대칭 상호작용 규칙.
- 지표 확장 — 극단값 비율, 중도 소멸도, 집단별 분포 격차.

**공통 함정 (기획서에서 이어짐):** 결과를 규칙에 미리 박지 않는다. 미세한 비대칭만
넣고 그것이 거시 격차로 증폭되는지를 관찰한다. 증폭되지 않아도 유의미한 결과다.

**저장소 노출 주의:** 집단 변수 실험을 할 때 클래스명을 `AsymmetricGroupRule`처럼
중립적으로 둔다. 코드는 맥락 없이 읽히기 쉽고, README에 특정 집단명이 박혀 있으면
의도와 다르게 읽힌다. 실험은 하되 해석은 결과 문서에서 설명한다.

---

## 9. M6 — (선택) Python 분석 레이어

> Java 엔진(시뮬) + Python(분석) 파이프라인. **병행 금지 — M3까지 끝난 뒤 착수.**

- [ ] 여러 시드·파라미터 조합 배치 실행 → 결과 CSV/JSON 덤프
- [ ] pandas/matplotlib으로 조건별 비교, 상전이 지점 통계 확인

---

## 10. 남은 미확정 결정

- [ ] 프로젝트명 확정 (`Polis` 유지 여부)
- [ ] M2 목표 N (권장: 10,000 — Canvas 렌더링과 WebSocket 대역폭이 실제로 문제가 되는 최소 규모)
- [ ] 저장 전략: 인메모리 유지 (시계열 DB는 넣지 않는 쪽으로 기울어 있음)

---

## 11. 잘라낸 것과 그 이유

기록으로 남긴다. 나중에 되살리고 싶어질 때 이 절을 먼저 읽는다.

| 잘라낸 것                           | 이유                                                                                                                           |
| ----------------------------------- | ------------------------------------------------------------------------------------------------------------------------------ |
| **3단계 외부 충격**                 | 특정 틱에 값을 흔드는 것 이상이 아니라 기술적 밀도가 낮다. 하고 싶어지면 M5 실험장에서 하면 된다.                              |
| ~~2단계 집단 변수~~                 | **철회 — M5로 이관.** 흥미 축이 살아 있으므로 잘라낼 이유가 없다. 노출 주의사항은 8절 참조.                                    |
| ~~Python 재구현~~                   | **철회 — M6으로 이관.** 여러 시드·파라미터 조합을 통계적으로 비교하려면 어차피 필요해진다. 다만 Java 쪽이 M3까지 끝난 뒤 착수. |
| **중앙 라우터(option b) 사전 도입** | 직접 참조 라우팅으로 충분한지는 M3-4에서 판단한다. 미리 넣는 것은 근거 없는 선반영.                                            |
| **harness `.mdc` 룰 파일 세트**     | `CLAUDE.md` 하나로 충분하다. 룰 파일을 여러 개 만드는 것 자체가 과잉 엔지니어링이었다.                                         |

---

## 12. 서사 점검 — 이 프로젝트를 한 문단으로 말한다면

> 정치적 양극화의 창발을 관찰하는 ABM 엔진을 Java로 구현했다. 핵심은 모델 자체가
> 아니라, **동일한 갱신 규칙 위에서 세 가지 동시성 모델을 구현하고 비교한 것**이다.
> 단일 스레드 동기 구현을 결정론적 앵커로 삼고, 틱 단위 병렬화가 수치적으로 동일한
> 결과를 내는지 검증한 뒤, 행위자당 영속 가상 스레드 모델을 추가해 전역 배리어를
> 제거했다. 마지막 모델에서만 백프레셔가 발생하는 이유, 그리고 이 CPU 집약 워크로드에서
> 가상 스레드가 유리한지 불리한지를 수치를 들어 설명할 수 있다.

이 문단이 방어되지 않는 작업은 우선순위가 낮다.

---

### 오늘의 작업

```
날짜:
목표(1~3개):
  -
  -
끝난 것:
막힌 것 / 내일로:
```
