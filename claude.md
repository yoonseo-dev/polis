# CLAUDE.md — Polis ABM 시뮬레이터

행위자 기반 모델링(ABM, Agent-Based Modeling) 기법으로 사회 현상을 재현하는 시뮬레이션 시스템이다. 실제 사람을 대상으로 하지 않고, 각자 내부 상태(예: 정치적 의견)와 행동 규칙을 가진 가상의 행위 주체를 코드로 수천~수만 개 생성한 뒤, 시간을 흐르게 하며 그들 사이에서 어떤 거시적 패턴이 떠오르는지 관찰한다.
행위자 개개인은 "내 주변 이웃들의 의견을 보고 내 의견을 조금 조정한다" 정도의 단순한 규칙만 따르는데, 수만 명이 모이면 사회 전체 수준에서는 의견이 양극단으로 갈라지거나, 특정 집단이 한 방향으로 쏠리거나, 중도가 소멸하는 등 누구도 의도하지 않은 패턴이 나타난다. 이 간극(미시 규칙 ↔ 거시 패턴)을 들여다보는 것이 이 프로젝트의 지적 핵심이다.

---

## 0.규칙

## 1. 기술 스택

| 덩어리                 | 기술                                                                     |
| ---------------------- | ------------------------------------------------------------------------ |
| 시뮬레이션 엔진 (핵심) | **순수 Java 21+** · 가상 스레드 · `java.util.concurrent` (Spring 미사용) |
| 중계·API 서버          | Spring Boot · WebSocket(STOMP)                                           |
| 시각화 프론트          | React · Canvas · D3.js                                                   |
| 저장                   | 초기 인메모리 → 추후 시계열 DB                                           |

- 메시지 큐는 외부 브로커(Kafka 등) 없이 **언어 내장 큐**로 시작.
- 행위자 수만 개라 DOM 불가 → 시각화는 **Canvas 필수**.

---

## 2. 아키텍처 — 갱신 규칙을 전략 패턴으로 분리

엔진 골격(틱 루프)·인프라는 고정. 단계가 바뀌면 ① 상태 정의 ② **갱신 규칙** ③ 측정 지표만 바뀐다. 갱신 규칙을 전략 패턴으로 분리해 구현체 교체만으로 모델을 전환한다.

- `UpdateRule` 인터페이스 — 입력(자기 상태 + 만난 이웃들) → 다음 상태 반환.
- 구현체: `AttractionRepulsionRule`(0단계), `EchoChamberRule`(1단계), `GroupCleavageRule`(2단계).
- 엔진은 `UpdateRule`만 알고 구현체를 모른다.

### 패키지 구조

```
polis-engine/            (순수 Java, 핵심)
  agent/
    Agent.java           행위자 — 상태 + 메일박스
    AgentState.java      opinion 등 상태값 (불변 객체 권장)
    Mailbox.java         BlockingQueue 래핑
    Message.java         행위자 간 전달 메시지
  rule/
    UpdateRule.java      전략 인터페이스
    AttractionRepulsionRule.java
  world/
    World.java           전체 행위자 보유 + 이웃 선택 로직
    Simulation.java      틱 루프, 가상 스레드 실행기 관리
  metric/
    MetricCollector.java 분산·히스토그램·극단값 비율 수집
  Main.java              M0: 콘솔에서 숫자로 결과 확인

polis-server/            (Spring Boot) — WebSocket, 상태 스트리밍, 제어 API
polis-front/             (React) — Canvas 시각화, 파라미터 제어 UI
```

---

## 3. 현재 작업: M0 구현 사양

> M0 = 순수 Java, 행위자 ~100개, 콘솔 출력, 0단계 규칙, **단일 스레드 동기 업데이트**.
> 검증: 콘솔에서 의견값이 양극으로 갈라지는지 확인.

### 3-1. 행위자 상태 (최소)

- `opinion: double` — 정치 의견값, 범위 **[-1.0, +1.0]** (-1=극좌, 0=중도, +1=극우).
- `id: int` (선택)
- `neighbors` (선택) — M0에서는 "매 틱 무작위로 일부와 만난다"로 단순화 가능.
- 성향 파라미터는 M0에서 **전역 상수**로 시작(변수 최소화).

### 3-2. 갱신 규칙 — 동화 + 반발

두 행위자 i, j가 만났을 때 `d = |opinion_i − opinion_j|`:

- **동화(d ≤ 임계값):** `opinion_i += μ × (opinion_j − opinion_i)`
- **반발(d > 임계값):** `opinion_i −= μ × (opinion_j − opinion_i)`
- 갱신 후 항상 **[-1, +1]로 clamp**.

핵심 파라미터: `μ`(학습률 0~1), 임계값(threshold). 반발 항이 있어야 양극화가 창발하고, 동화만 있으면 평균값으로 수렴한다 — 실험으로 직접 확인.

### 3-3. 측정 지표

- opinion 히스토그램(봉우리 1개 vs 2개).
- 분산(variance) 또는 극단값 점유율(예: `|opinion| > 0.8` 비율).
- 틱에 따른 위 지표 추세.

### 3-4. 업데이트 방식

- **M0 = (A) 틱 기반 동기 업데이트.** 매 틱 모든 행위자가 "현재 상태"를 읽고 "다음 상태"를 계산 후 일괄 반영(double-buffering). 결정론적이라 검증 쉬움. **먼저 단일 스레드로 규칙 검증**, 이후 가상 스레드로 병렬화(읽기=이전 버퍼, 쓰기=다음 버퍼라 경쟁 적음).
- **M1 = (B) 행위자당 영속 가상 스레드 + 메일박스 큐.** 비결정적이므로 A로 정답지 확보 후 전환.

### 현재 여기까지 완성됨!

---

## 4. 동시성 도구 (M1부터 본격화)

- 실행기: `Executors.newVirtualThreadPerTaskExecutor()`
- 메일박스: 행위자마다 `BlockingQueue<Message>`(예: `LinkedBlockingQueue`). 백프레셔 — bounded queue로 막거나 드롭/병합 정책 실험.
- 공유 상태: `ConcurrentHashMap`, `LongAdder`, `AtomicReference`. 가능하면 공유 최소화(로컬 상태 + 메시지 전달)로 레이스 컨디션 자체를 회피.
- 틱 동기화(A 방식): `invokeAll` 또는 `CompletableFuture.allOf`로 한 틱 완료 대기 후 버퍼 스왑.

---

## 5. 마일스톤

|               | 내용                                                 | 검증                       |
| ------------- | ---------------------------------------------------- | -------------------------- |
| **M0** ← 현재 | 순수 Java, 100개, 콘솔, 0단계 규칙, 단일 스레드 동기 | 콘솔에서 양극화 확인       |
| M1            | 수만 개 확장, 메일박스 큐, 영속 스레드 모델          | 대규모 안정 구동, 백프레셔 |
| M2            | Spring Boot+WebSocket push, React+Canvas             | 실시간 양극화 관찰         |
| M3            | 갱신 규칙 전략 패턴화, 1단계(극단화) 추가            | 0↔1단계 전환               |
| M4            | 2단계(성별 균열)                                     | 집단별 분포 격차 측정      |
| M5 (선택)     | 3단계(외부 충격)                                     | 충격 전후 변화             |

**진행 철칙:** 엔진을 콘솔 숫자로 검증한 _뒤_ 시각화를 얹는다.

### 단계별 갱신 규칙 (전략 구현체 명세)

- **1단계 극단화:** 선택적 노출(에코 챔버), 집단 극화. 지표 = 극단값 비율, 중도 소멸도.
- **2단계 성별 균열:** "남성=우, 여성=좌"를 규칙에 박지 말 것(동어반복). **미세한 비대칭**만 넣고 거시 격차로 증폭되는지 관찰. 지표 = 집단별 분포 격차.

---

## 6. 미확정 (코드에 영향, 작업 전 확인)

- 행위자 공간 구조: 격자(grid) vs 관계망(network) vs 무작위 만남.
- M0 행위자 수, M1 목표 확장 규모.
- 저장 전략: 인메모리 → 시계열 DB 도입 시점.
- 0단계 파라미터(μ, 임계값) 초기값·실험 범위.
